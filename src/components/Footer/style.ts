import styled from 'styled-components'

export const Container = styled.footer`
	width: 100%;
	background-color: var(--yellow);
	margin-top: 5rem;
	padding: 0.5rem;

	p {
		text-align: center;
		color: #000;
		font-size: 0.9rem;
	}
	

`